CC = gcc
CFLAGS = -Wall -Wextra -std=gnu11 -g -I$(CJSONDIR)
LDFLAGS = -lcurl

SRCDIR = .
CJSONDIR = ./cJSON
SRC = $(SRCDIR)/alice.c $(SRCDIR)/core/conections.c $(SRCDIR)/core/handle_json.c $(CJSONDIR)/core/cJSON.c $(SRCDIR)/core/cryptografy.c
OBJS = $(SRC:.c=.o)
TARGET = alice

.PHONY: all clean run

all: $(TARGET)

$(TARGET): $(OBJS)
	$(CC) $(CFLAGS) -o $@ $^ $(LDFLAGS)

%.o: %.c
	$(CC) $(CFLAGS) -c $< -o $@

clean:
	rm -f $(OBJS) $(TARGET)

run: $(TARGET)
	./$(TARGET)