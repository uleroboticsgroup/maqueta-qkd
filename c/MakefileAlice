CC = gcc
CFLAGS = -Wall -Wextra -std=gnu11 -g -I$(CJSONDIR)
LDFLAGS = -lcurl

SRCDIR = .
CJSONDIR = ./cJSON
SRC = $(SRCDIR)/alice.c $(SRCDIR)/core/connection_qkd.c $(SRCDIR)/core/handle_json.c $(CJSONDIR)/cJSON.c $(SRCDIR)/core/cryptografy.c $(SRCDIR)/core/handle_msg.c
OBJS = $(SRC:.c=.o)
TARGET = alice

.PHONY: all clean run

all: $(TARGET)

$(TARGET): $(OBJS)
	$(CC) $(CFLAGS) -o $@ $^ $(LDFLAGS)

%.o: %.c
	$(CC) $(CFLAGS) -c $< -o $@

clean:
	rm -f $(OBJS) $(TARGET)

run: $(TARGET)
	./$(TARGET)